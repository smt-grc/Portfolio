"""grctoolkit - a small package that bundles the GRC tools from this course
behind a single command-line entrypoint (`grctool`).

This file makes `grctoolkit` a Python *package*. Keeping each tool in its own
module (passwords, access, evidence) and registering it with the CLI is exactly
how real multi-command tools (git, aws, kubectl) are organized.
"""

__version__ = "0.1.0"
